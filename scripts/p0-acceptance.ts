import assert from "node:assert/strict";
import { normalizeAgentTaskResult } from "../lib/agent-result-store.ts"; import { candidateFingerprint,createExperienceCandidate,matchReviewedExperience } from "../lib/evolution-store.ts"; import { buildP0Metrics } from "../lib/eval.ts"; import { mergeLearningRun,normalizeLearningRun,shouldPollLearningRun,type LearningRun } from "../lib/learning-run-store.ts"; import { buildAgentMaterialContext,isLocalTextFile,type LocalMaterial } from "../lib/material-store.ts"; import { normalizeMaterialUploadAck } from "../lib/material-upload.ts"; import { buildP0Bundle,parseP0Bundle } from "../lib/p0-bundle.ts"; import { parsePersistedValue,serializePersistedValue } from "../lib/persistence.ts"; import { isProductOperation,validateStableEntityAck } from "../lib/product-contract.ts"; import { LOCAL_MARKETING_SESSION,hasMarketingCapability,hasWorkspaceAccess,normalizeMarketingSession } from "../lib/product-session.ts"; import { reconcileSnapshots,snapshotFingerprint,validateEntityReadResponse } from "../lib/reconcile.ts"; import { logicalStorageKeyForScope,scopedStorageKeyForScope,storageScopeForSession } from "../lib/storage-scope.ts"; import { syncRecordFromResponse } from "../lib/sync-store.ts"; import { buildTaskExecutionState,taskExecutionId } from "../lib/task-execution.ts";
function candidate(input:{taskId:string;workspaceId:string;taskType:string;outcome:string;note?:string}){const fingerprint=candidateFingerprint({finalText:`final-${input.taskId}`,feedback:"采用",outcome:input.outcome,outcomeNote:input.note??""});return createExperienceCandidate({taskId:input.taskId,workspaceId:input.workspaceId,taskType:input.taskType,taskGoal:"推进下一阶段",artifactTitle:"测试产出",feedback:"采用",outcome:input.outcome,outcomeNote:input.note??"",editCount:3,fingerprint});}
const positive=candidate({taskId:"t1",workspaceId:"w1",taskType:"策略判断",outcome:"方案采用",note:"客户确认进入下一轮"});const failure=candidate({taskId:"t2",workspaceId:"w1",taskType:"策略判断",outcome:"失败",note:"过早进入价格讨论"});assert.equal(positive.polarity,"positive");assert.equal(failure.polarity,"negative");const reviews={[positive.id]:"accepted",[failure.id]:"accepted"};const matched=matchReviewedExperience({candidates:[positive,failure],reviews,workspaceId:"w2",taskType:"策略判断"});assert.equal(matched.experiences.length,1);assert.equal(matched.counterexamples.length,1);assert.equal(matchReviewedExperience({candidates:[positive],reviews,workspaceId:"w1",taskType:"会前准备"}).experiences.length,0);
const metrics=buildP0Metrics([{taskId:"a",taskType:"策略判断",feedback:"修改后采用",outcome:"方案采用",editCount:8,appliedExperienceCount:0},{taskId:"b",taskType:"策略判断",feedback:"采用",outcome:"项目推进",editCount:3,appliedExperienceCount:1}]);assert.equal(metrics.outcomeSuccessRate,1);assert.equal(metrics.experienceReuseRate,0.5);assert.equal(metrics.repeatedTaskTypes[0].editDelta,-5);
assert.deepEqual(parsePersistedValue(JSON.stringify({hello:"legacy"}),{}),{hello:"legacy"});assert.deepEqual(parsePersistedValue(serializePersistedValue({hello:"v1"}),{}),{hello:"v1"});const bundle=buildP0Bundle({"marketing:task":"x","unrelated:key":"secret"},"2026-09-02T00:00:00.000Z");assert.deepEqual(bundle.entries,{"marketing:task":"x"});assert.equal(parseP0Bundle(JSON.stringify(bundle)).entries["marketing:task"],"x");
const platformSession=normalizeMarketingSession({tenant_id:"tenant-a",tenant_name:"团队 A",actor_id:"user-1",actor_name:"Alice",roles:["member"],capabilities:["workspace.read","task.create"],workspace_grants:[{workspace_id:"w1",access:"write"}],team_enabled:true});assert.ok(platformSession);assert.equal(hasMarketingCapability(platformSession!,"task.create"),true);assert.equal(hasWorkspaceAccess(platformSession!,"w1","write"),true);assert.equal(hasWorkspaceAccess(platformSession!,"w1","admin"),false);assert.equal(hasMarketingCapability(LOCAL_MARKETING_SESSION,"team.manage"),false);const platformScope=storageScopeForSession(platformSession!);const scoped=scopedStorageKeyForScope("marketing:tasks",platformScope);assert.equal(scoped,"marketing:scope:tenant-a:user-1:tasks");assert.equal(logicalStorageKeyForScope(scoped,platformScope),"marketing:tasks");assert.equal(logicalStorageKeyForScope("marketing:scope:tenant-b:user-2:tasks",platformScope),null);assert.equal(scopedStorageKeyForScope("marketing:tasks",storageScopeForSession(LOCAL_MARKETING_SESSION)),"marketing:tasks");
assert.equal(isLocalTextFile({name:"meeting.md",type:"text/markdown"}),true);assert.equal(isLocalTextFile({name:"proposal.pdf",type:"application/pdf"}),false);const materialSamples:LocalMaterial[]=[{id:"m1",title:"meeting.md",kind:"MD",source:"local",status:"Ready",parseMode:"local_text",content:"ABCDEFGHIJ",createdAt:"now"},{id:"m2",title:"proposal.pdf",kind:"PDF",source:"local",status:"AWKN 已解析",parseMode:"platform_parsed",content:"PARSED",evidence:[{type:"PDF",title:"第 1 页",snippet:"关键结论",source:"proposal.pdf"}],createdAt:"now"}];const agentMaterials=buildAgentMaterialContext(materialSamples,11);assert.equal(agentMaterials[0].content,"ABCDEFGHIJ");assert.equal(agentMaterials[1].content,"P");assert.equal(agentMaterials[1].evidence?.[0].snippet,"关键结论");
const uploaded=normalizeMaterialUploadAck({ok:true,trace_id:"trace-material",data:{material_id:"m2",parse_status:"completed",parsed_text:"已解析正文",evidence:[{type:"PDF",title:"第 1 页",snippet:"关键证据",source:"proposal.pdf"}]}},"m2");assert.equal(uploaded.ok,true);assert.equal(uploaded.state,"ready");assert.equal(uploaded.evidence.length,1);assert.equal(normalizeMaterialUploadAck({ok:true,data:{material_id:"m2",parse_status:"processing"}},"m2").state,"parsing");assert.equal(normalizeMaterialUploadAck({ok:true,data:{material_id:"other",parse_status:"ready"}},"m2").error?.code,"MATERIAL_IDENTITY_MISMATCH");assert.equal(normalizeMaterialUploadAck({ok:false,error:{code:"PLATFORM_NOT_CONFIGURED",message:"local"}},"m2").state,"local-only");assert.equal(isProductOperation("material.parse.get"),true);assert.equal(isProductOperation("material.parse.retry"),true);
const baseline={id:"w1",name:"A"};const baselineFp=snapshotFingerprint(baseline);assert.equal(reconcileSnapshots({localSnapshot:baseline,platformSnapshot:baseline,platformRevision:1,baselineFingerprint:baselineFp,baselineRevision:1}).state,"clean");assert.equal(reconcileSnapshots({localSnapshot:{id:"w1",name:"B"},platformSnapshot:baseline,platformRevision:1,baselineFingerprint:baselineFp,baselineRevision:1}).state,"local-newer");assert.equal(reconcileSnapshots({localSnapshot:baseline,platformSnapshot:{id:"w1",name:"C"},platformRevision:2,baselineFingerprint:baselineFp,baselineRevision:1}).state,"platform-newer");assert.equal(reconcileSnapshots({localSnapshot:{id:"w1",name:"B"},platformSnapshot:{id:"w1",name:"C"},platformRevision:2,baselineFingerprint:baselineFp,baselineRevision:1}).state,"conflict");assert.equal(validateEntityReadResponse({ok:true,data:{entity_id:"other",revision:2,entity:{id:"other"}}},"w1").error?.code,"ENTITY_READ_IDENTITY_MISMATCH");
const execution=buildTaskExecutionState({taskId:"t1",workspaceId:"w1",artifactTitle:"方案",finalText:"最终稿",feedback:"采用",outcome:"项目推进",outcomeNote:"进入下一轮"});assert.equal(execution.id,taskExecutionId("t1"));assert.equal(isProductOperation("task.execution.get"),true);assert.equal(isProductOperation("task.execution.upsert"),true);const executionFp=snapshotFingerprint(execution);assert.equal(reconcileSnapshots({localSnapshot:{...execution,finalText:"本地改"},platformSnapshot:{...execution,finalText:"平台改"},platformRevision:2,baselineFingerprint:executionFp,baselineRevision:1}).state,"conflict");
const agentResult=normalizeAgentTaskResult({text:"明确判断",trace_id:"trace-1",evidence:[{type:"WEB",title:"来源 A",snippet:"关键证据",source:"https://example.com"}],artifact:{title:"策略稿",markdown:"# 第一版\n内容"}},"2026-09-02T00:00:00.000Z");assert.ok(agentResult);assert.equal(agentResult?.evidence.length,1);const learningRun=normalizeLearningRun({data:{run_id:"lr-1",status:"running"},workspaceId:"w1",watchId:"watch-w1",traceId:"trace-learning",startedAt:"start"});assert.ok(learningRun);assert.equal(shouldPollLearningRun(learningRun!),true);const completed=normalizeLearningRun({data:{run_id:"lr-1",status:"completed",signals:[{title:"政策变化",summary:"出现新要求",why_it_matters:"影响当前方案",action:"更新提案",source:"gov.example"}]},workspaceId:"w1",watchId:"watch-w1",startedAt:"other"});assert.ok(completed);const merged=mergeLearningRun(learningRun!,completed!);assert.equal(merged.status,"completed");assert.equal(merged.startedAt,"start");assert.equal(merged.signals.length,1);const partialPrevious:LearningRun={...merged,status:"running",signals:merged.signals};const emptyRunning=normalizeLearningRun({data:{run_id:"lr-1",status:"running",signals:[]},workspaceId:"w1",watchId:"watch-w1",startedAt:"start"})!;assert.equal(mergeLearningRun(partialPrevious,emptyRunning).signals.length,1);assert.equal(isProductOperation("learning.run.get"),true);assert.equal(isProductOperation("learning.run.retry"),true);
assert.equal(syncRecordFromResponse({entityKey:"task:t1",operation:"task.create",response:{ok:true,trace_id:"trace-sync"},updatedAt:"now",syncedFingerprint:"fp-test"}).state,"synced");assert.equal(syncRecordFromResponse({entityKey:"task:t1",operation:"task.create",response:{ok:false,error:{code:"PLATFORM_NOT_CONFIGURED",message:"local"}},updatedAt:"now"}).state,"local-only");assert.equal(validateStableEntityAck({ok:true,data:{entity_id:"w1",revision:2}},"w1").ok,true);assert.equal(validateStableEntityAck({ok:true,data:{entity_id:"other"}},"w1").error?.code,"IDENTITY_MISMATCH");
console.log("P0 acceptance passed: session scope isolation, learning polling, task execution reconciliation, material lifecycle, evolution, agent results and sync boundaries are enforced.");
